server {
    listen 80;
    listen [::]:80;
    server_name dev.koinvote.com;  # 或 koinvote.com for production

    # API 代理 - 必須放在 location / 之前
    location /api/v1/ {
        proxy_pass http://35.229.204.234:8080/api/v1/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # 靜態資源
    location /assets/ {
        alias /var/www/koinvote/dist/assets/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # SPA 路由 - 所有其他請求返回 index.html
    location / {
        root /var/www/koinvote/dist;
        try_files $uri $uri/ /index.html;
    }
}

